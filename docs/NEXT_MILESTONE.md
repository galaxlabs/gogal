# Next Milestone

## Current Milestone
Stabilize metadata core and setup reliability.

## Targets
1. Verify PostgreSQL privilege automation (`doctor` + `fix-postgres`).
2. Complete DocType Builder -> table migration flow.
3. Improve Desk record form/list generation from metadata.
4. Add non-destructive migration planner details (indexes and constraints plan output).

## Acceptance
Create `Customer` DocType, auto-create `tab_customer`, create/list/edit records via API and Desk.
